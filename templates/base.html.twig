<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>{{ block('title') }} - Adresář kontaktů</title>
		{% block stylesheets %}{% endblock %}
	</head>
	<body>

		<div class="page-header">
			<small>BlueGhost - praktický test na znalosti PHP</small>
		</div>
		<h1>{{ block('title') }}</h1>

		{% for label, messages in app.flashes %}
			{% for message in messages %}
				<div class="flash-{{ label }}">
					{{ message }}
				</div>
			{% endfor %}
		{% endfor %}

		{% block body %}{% endblock %}

		<div class="footer">
			<hr/>
			<small>Karel Zavadil &copy 2020</small>
		</div>

	</body>
</html>
